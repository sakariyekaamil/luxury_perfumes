# Local dev uses Vite proxy — leave unset or point at local backend
# VITE_API_URL=http://localhost:5000/api

# Production backend (also set in Vercel frontend project env vars)
# VITE_API_URL=https://luxury-perfumes-three.vercel.app/api
